language: node_js
node_js:
  - 10

services: 
  - docker


before_install:
  - docker build -t video-hooks:latest .
  - docker images

deploy:
  provider: script
  script: bash docker_push
  on:
    branch: master